# SAGE 依赖清单
# 安装命令：pip install -r requirements.txt

# ---- 核心依赖 ----
# 本地小模型推断（HuggingFace）
transformers>=4.40.0
torch>=2.1.0
# 量化支持（可选，仅 CUDA 环境）
# bitsandbytes>=0.43.0

# ---- API 客户端 ----
# 异步 HTTP（Oracle API 调用）
httpx>=0.27.0

# ---- 工具 ----
# 异步支持
asyncio; python_version < "3.10"

# ---- 可选：NLP 增强 ----
# 中文分词（可替换 extract_concepts 中的简单分词）
# jieba>=0.42.1
# 英文 NER
# spacy>=3.7.0

# ---- 开发 / 测试 ----
pytest>=8.0.0
pytest-asyncio>=0.23.0
